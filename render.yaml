services:
  - type: web
    name: gnn-polymer-prediction
    env: python
    buildCommand: |
      pip install -r requirements.txt
      # roda o script de treino para gerar model/simple_gnn.pt
      python gnn_polymer_predictor.py
    startCommand: uvicorn main:app --host=0.0.0.0 --port=$PORT
